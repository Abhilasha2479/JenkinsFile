pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                @ECHO OFF
                ECHO Build is successfull
                PAUSE
            }
        }
        stage('Test') {
            steps {
                @ECHO OFF
                ECHO Test is successfull
                PAUSE
            }
        }
        stage('Deploy') {
            steps {
                @ECHO OFF
                ECHO Deploy is successfull
                PAUSE
            }
        }
    }
}
